package com.footballdemo.football_family.model;

public enum EventType {
    CLUB_EVENT,   // Événements liés à un club (entraîneur, coach, club admin)
    OPEN_EVENT    // Événements ouverts au public / amateurs / joueurs indépendants
}
