package com.footballdemo.football_family.model;

public enum ClubVerificationStatus {
    PENDING,        // Club en attente de vérification
    APPROVED,       // Club validé par l'équipe UTF
    REJECTED        // Refusé (documents incorrects)
}
